# omniplc 变更历史

按版本号降序排列(新版本在前);每条注明该版本的实质改动与文档出处。
当前发布版本见 Git 标签(`git tag -l 'v*'`);CI 自动构建产物发到 PyPI 与对应 GitHub Release。

---

- **v0.41.0**:现场评审必修批(8 项代码修复 + 自审 7 处 + CI 版本矩阵)——(1)**MC 位软元件位号后缀静默丢弃(P0)**:`M10.5` 原被当 `M10` 读写,位号丢失且无任何报错;校验下沉组帧层 `codec_qna.build_core` / `codec_a.build_request`,并补齐串口 3C(`codec_serial._ascii_core`)/ 1C(`codec_serial_a.build_1c_request`)/ 0406 批量位块——同步 / native / 串口全帧型共用同一防线;品牌子类(松下 `R1.15`、汇川 `X17` 八进制)在 `_build_frame` 内先换算记号,不受影响。(2)**MC 设备码表扩容**:补 L/F/SB/V/DX/DY/TS/TC/TN/CS/CC/CN/SM/SD/SW(定时器·计数器·特殊继电器·链接寄存器);G(智能模块缓冲存储器)非 SLMP 软元件寻址,不收录;TN/CN 码值标注待真机终核。(3)**NJ BOOL 数组**:`OmronCipClient` 覆写元素读 / 写——元素直读、实际类型由自描述应答决定,应答存储字(DWORD)时按 Logix `下标//32` 口径回退。(4)**NJ STRING**:按 `len(u32)+字符` 布局实现读写,写前先读模板实例号与声明尺寸、写入类型域回带实际模板号、值超声明尺寸拒绝。(5)**AB 0x0A 字节预算**:`read_batch` 改按 (条数 ≤32, 估算字节 ≤480B) 双约束自动拆事务,长标签名不再触顶未连接缓冲;条数硬上限取消。(6)**S7 STRING**:读实际长超请求 `length` 时按 `length` 截断返回(原静默返空串丢数据);写保留 PLC 侧声明长字节、仅覆盖实际长,超声明长拒绝(防溢出污染相邻变量),未初始化区(声明长 0)兼容旧口径。(7)**ADS transport 错误分流**:0x705/0x706/0x725 归内部异常(标记断线走惰性重连),其余 `ADSError` 仍为 `DeviceError` 不断线。(8)**FX5U X/Y 八进制**:新增 `xy_octal` 构造参数(同步 TCP/UDP + native 同名参数),默认仍为 Q/L/R 十六进制口径。自审另修 7 处(位号后缀错误消息单一来源、串口 3C/1C 校验漏网、NJ 批量地址钩子不自洽、死代码 / 文档标注 / 无效测试清理)。(9)**CI 版本矩阵**:按"3.7 必保(现场老设备 vendor SDK 依赖)、3.12 覆盖新环境(能装 3.8~3.11 的设备同样兼容 3.12)"裁决,CI 改矩阵 `["3.7.9", "3.12"]`(3.7 腿用 `actions/setup-python` 安装、`UV_PYTHON_DOWNLOADS=never` 禁 uv 静默换版本、显式 `--python`;ruff/mypy/ty 仅 3.12 腿)。**行为变更提示**:S7 写 STRING 的声明长字节不再被覆盖(此前写坏声明 max);AB `read_batch` 超 32 条不再报错而是自动拆分;MC `.bit` 后缀对位软元件由静默错位改为 `ValueError`。真机核证待做:MC 新设备码(尤其 TN/CN)、ADS 三码、NJ STRING 模板 / 布局、AB 480B 预算余量。门禁:3.7.9 **1129 passed** / ruff / mypy(76 files) / ty 全零;`docs/review.md` 入库(复核 v2 + 修复记录,替换旧审核台账,旧稿保留在 git 历史)。
- **v0.40.1**:补丁——修掉 3.8+ 上取消异常口径导致的静默失效(库缺陷)与 3.12 下的测试收尾挂死(脚手架)——(1)**取消异常口径跨解释器统一(影响面:所有 3.8+ 环境)**:`from concurrent.futures import CancelledError` 只在 3.7 成立——那一版它与 `asyncio.CancelledError` 是**同一个类**;3.8 起 asyncio 的取消异常改为自己的 `BaseException` 子类,两者**不再是同一个类**(3.12 实测身份比较为 False),于是 native 层与 aio 层所有 `except CancelledError` 在 3.8+ 全部**漏网**:①`_await_with_timeout` 超时路径取消内层任务后,`await task` 抛出的 CancelledError 不被吞 → 超时**不再**翻译成 `socket.timeout`/`TransportTimeoutError`,而是把取消异常抛给事务层;②`_execute` 取消分支不再执行 → 取消后仍报 `connected`,链路不做保守拆连;③`_connect_locked` 两处取消分支不清理半开传输;④aio `close()` 的取消回退(`shutdown(wait=True)`)不再触发。**本地 .venv 钉 3.7.9,天然看不到;由 CI 的 3.12 腿抓到(实测 7 例 FAILED)**。修法:`core/errors.py` 给出跨代元组 **`_CANCELLED_ERRORS`**(同时含 futures 版与 `getattr(asyncio, "CancelledError")`——后者不能直接 `import`:3.11+ 的类型库把它建模成内建名,ty 会报"模块无此成员";它**也不是** `builtins` 属性,本次修复的第一版就栽在这个误判上),native 基类/传输层与 aio 三层统一引用;需要连普通异常一起吞时用 `_CANCELLED_ERRORS + (Exception,)`。(2)**3.12 下测试收尾挂死(脚手架问题,非库缺陷)**:修好(1)后 3.12 上整套用例卡在 71% 不动(CI 那轮因此被 15 分钟超时取消)——3.12 起 `Server.wait_closed()` 还会等**每个连接由应用侧关净**,而"接受但不回应"的测试 handler(读超时类用例必需的形态,`lambda r, w: None` 从不关 writer)让它永久挂住(纯标准库即可复现:`lambda` 形态 2 秒不返回,协程 handler 里 `w.close()` 正常返回);3.7 只等监听套接字关闭,故本地从未暴露。修法:`tests/unit/scripted_async.py` 新增 `close_server(server)`(只 `close()`,不 await `wait_closed()`),`RawTcpServer.stop()` 改为"先取消在等数据的连接任务 → 再关服务端"并给等待加 1 秒上限。(3)**守卫**:新增 `test_cancelled_error_covers_asyncio_class`(断言口径含 asyncio 那一条、且 native 两模块与 aio 共用同一份对象)。门禁:3.7.9 **1113 passed** / 3.12.10 **1113 passed**(CI 腿本地复现)/ ruff / mypy(76 files) / ty 全零;GitHub CI 全绿。
- **v0.40.0**:AB 会话泄漏修复 + 原生异步层复查收口 + K1 复核更正(审计结论固化为门禁)——(1)**AB EtherNet/IP 连接初始化失败时注销 CIP 会话**:`_after_connect` 注册会话后 Forward Open 全失败时,基类清理路径只关传输、不走 AB 覆写的 `disconnect()`,注册过的会话要等 PLC 侧超时才回收(ControlLogix 典型 ≤16),反复失败重连可耗尽会话表(服务端会话表 DoS);现新增基类钩子 **`_after_connect_failure()`**(同步 + 原生镜像,默认空操作),在 `_after_connect()` 失败分支、**传输关闭之前**调用——放关闭之后等于白修:注销帧发不出去(`TransportClosedError` 属 `OmniPLCInternalError`,会被 `_unregister_session` 的 except 吞掉),实测线上零字节;AB 覆写为尽力 `_forward_close()` + `_unregister_session()`(失败路径 `_ot_connection_id` 必为 None,Forward Close 自然空转)。**行为变更提示**:连接初始化失败时线尾会多一帧 UnregisterSession(正常断开路径不变)。(2)**原生层两处真缺陷**(本轮复查发现并修复):①**取消 UDP 读后关闭套接字会把事件循环带崩**——3.7 取消 `sock_recv_into`/`sock_sendall` 时不会立即摘除 selector 注册(要等该 fd 下次就绪才自清理),而 `_clear_stale_selector` 原先只在发/收**超时**路径调用,**取消**路径(`_execute` 按"已发出请求"保守拆连 → `_mark_disconnected` → `close()`)与用户直接 `close()`/`disconnect()` 全都绕过它,关句柄后 `select()` 对已关闭句柄抛 `WSAENOTSOCK`(Windows 10038;POSIX `EBADF`)并从 `run_until_complete()` 逃逸(3.7 在 Windows 上的**默认**事件循环就是 Selector → 默认配置下的可达路径;Proactor 不受影响);现收口到 `AsyncUdpTransport.close()`(关句柄前摘 reader + writer,幂等;`_clear_stale_selector` 对假 socket 的 `fileno` 缺失也容错),一次覆盖超时/取消/显式关闭全部路径,另一症状"Task was destroyed but it is pending"随之消失。②**FINS/UDP 主机名目标的节点号推导阻塞事件循环**——`_after_connect` 把主机名直接交给 `_node_from_host`(内部 `socket.gethostbyname`)与 `_local_ip_for`(内部 `probe.connect((host, port))` 同样要解析),而传输层 `connect()` 其实已用 `loop.getaddrinfo` 异步解析过一次、结果被丢弃;实测(把 `gethostbyname` 换成 0.25s 慢实现)事件循环被卡住 **260ms**,慢 DNS 更久,与该层"不阻塞事件循环"的定位冲突;现 UDP 传输把已解析的对端 IP 以新增的 **`AsyncBaseTransport.peer_ip`**(`AsyncUdpTransport` 连接后给出字面量、`close()` 后回 `None`)暴露,FINS/UDP 节点推导一律用它(取不到时回退 `_ip_address`),IP 字面量目标本就无解析步骤 → **取值与同步层逐值一致**;driver docstring 里"不发报文、无网络等待"的错误表述删除。(3)**K1(Keyence MC 位组地址)复核更正,不改行为**:原报告"决定性反证"(R199915 装不进设备号字段)**不成立**——3E 二进制的设备号(head device)字段是 **3 字节小端**(`plc/melsec/codec_qna.py:125,407` 的 `number.to_bytes(3, "little")`),199915 = 0x030CEB 装得下;反向证据存在:三菱 SLMP 手册 SH081257ENG 对"通信对象为 KEYENCE(KV 系列)"明文规定"除 B 设备外,设备格式由上位字号 + 低 2 位位号构成"(即线上编号 = 记号数字原样,`R515` → 515 = 本库现状),KV 公布的点数范围亦按该记号书写(`R00000~R199915`,未见线性 `0~31999` 的写法),本库码表 `B=十六进制 / R=十进制` 的分工正对应该条的"B 设备例外"。按线性改会让组号≥1 的 R 静默读写到别的继电器(风险不对称)→ 处置:**现状不改**,核证判据写入 `docs/real-machine-checklist.md`(写 `R100`,在 KV Studio 看 `R100`(组 1 位 0)还是 `R604`(组 6 位 4 = 线性 100)变化);`docs/review.md` §7.7-B 增第 8 条复核更正、C 段与两处状态表行同步(K2 记为已修);`keyence/mc.py` docstring 写明位组记号线上编码口径。(4)**测试与文档**:+34 例(1078 → **1112**)——AB 失败路径 6 例(含"关闭后拒发"假传输复刻真传输语义、20 次失败重连注册/注销帧数相抵、成功路径不污染、原生侧钩子先于关闭执行;旧实现上 3 例实测 FAILED)、原生取消/关闭 2 例(旧实现报 `OSError(10038)` FAILED)、FINS/UDP 主机名零阻塞 1 例(`gethostbyname` 换成"一调就炸",旧实现在该用例下连不上)、`peer_ip` 生命周期断言、**构造签名一致性守卫**(5 对孪生客户端 `__init__` 参数名 + 默认值)、**边界入参对拍 29 例**(Modbus/MC/FINS × 非法长度/空串/越界值/未知类型/坏地址/位区读字符串/字软元件无位号读 BOOL…断言两层"异常类型 + 文案 + 返回形状"完全相同——锁的是**校验时机**;变异验证:把原生 `read_string` 的 `length <= 0` 临时改成 `< 0`,三个协议的对应用例立即 FAILED);`architecture.md` §3 继承定制点补 `_after_connect_failure`、§12 补"主机名解析与节点推导"与 selector 残留的收口位置。门禁:pytest **1112 passed**(+ ruff / mypy 76 files / ty 全零)。
- **v0.39.0**:原生异步层落地 + 两批契约/审核收口——(1)**新增 `omniplc.native`(原生 asyncio 层,`Async*` 前缀)**,与 `omniplc.aio`(同步 I/O + 线程池包装)并存、后者继续覆盖全部协议作过渡。首批 5 个客户端:`AsyncModbusTcpClient`、`AsyncMelsecMcTcpClient`/`AsyncMelsecMcUdpClient`(A 兼容 1E / QnA 兼容 3E)、`AsyncOmronFinsTcpClient`/`AsyncOmronFinsUdpClient`;能力面 = 单点读/写 + 类型化方法 + `read_string`/`write_string` + 点位表(**批量与各家扩展方法留后续批次,缺口由守卫测试的表项锁定**)。**只重写薄分发层**:每个驱动的协议逻辑与传输交界只有 `_transact`,组帧/解析/地址/字序/错误分类助手(`_describe`/`_categorize`/`_extract_code`)/`ClientStats` 全部复用同步侧同一实现,不存在第二套帧语义。口径与同步层逐条对齐(惰性重连、退避门控、`retries`/`write_retries`、`is_write` 复用、超时分支、`DeviceError` 有码才计、`last_error` 三件套);**差异在**——属性是直接读字段、不取锁(单线程事件循环里是真原子快照,正是包装层"读属性抢事务锁最长阻塞一个 `receive_timeout`"的解法)、`asyncio.Lock` 非重入故只有公开入口取锁且**按首次使用时的事件循环惰性创建**(3.7 的 Lock 构造即绑循环)、**原生取消**真中断(取消时按"是否已发出请求"决定:已发出 → 保守拆连重同步,仅排队 → 保持连接,`CancelledError` 原样传播)。传输层:TCP 走 `open_connection` 流式读、`recv` 用绝对 deadline、超时抛 `socket.timeout`(与同步 TCP 同口径:分类 TIMEOUT 且拆连);**UDP 走已连接 socket + `loop.sock_recv_into`/`sock_sendall`**——本机实测 3.7 的 `ProactorEventLoop` **不支持** `create_datagram_endpoint`(`_make_datagram_transport` 默认 `NotImplementedError`,唯一实现在 selector 路径)且无 `sock_recvfrom`(3.11 才有),而这组 API 两种循环都有实现、3.7~3.13 通用;UDP **接收**超时抛 `TransportTimeoutError`(不拆连)、**发送**超时抛 `socket.timeout`(拆连),与同步收/发两条路径逐条对应。顺带修掉一个 3.7 缺陷:取消 `sock_recv_into`/`sock_sendall` 后 selector 注册不会立即摘除,期间关套接字会让 Windows 的 `select` 抛 `WSAENOTSOCK`(10038)带崩事件循环——超时路径显式摘 reader+writer。(2)**审核修复**(自审本轮代码,无 P0/P1):UDP 走线**主机名解析钉 `AF_INET`**——原取 `getaddrinfo` 首个结果,实测本机 `localhost` 首个即 `AF_INET6`,同一地址同一端口下同步层走 IPv4 通、原生层走 `::1` 报 `ConnectionResetError`,两层行为分裂(已复现并修);`_clear_stale_selector` 补 `remove_writer`(覆盖"发送缓冲打满"路径);对拍表扩到**全部 10 种数据类型** + MC/FINS 字符串读写 + 三协议的 `read_tag`/`write_tag`,并新增核心语义用例(读失败重连重发、**`write_retries=0` 时写失败只发 1 帧**、退避门控不污染 `error_count`、`TransportTimeoutError` 不拆连、连接期取消干净清场、`close()` 等锁不锯断在途事务)。(3)**契约矛盾批 2**:`write_bool` 守卫收窄为**只接受 bool 与 int 0/1**(此前 `write_bool(addr, 5)` 被 `bool()` 静默写成真值,而 docstring 写的是"兼容 int 0/1"),同步层与原生层一起改并加"两层行为一致"守卫用例;**MEWTOCOL 非数字错误码不再抛裸 `ValueError`**(`int(code)` 遇到表外的 `E1` 会穿透 `read()` 的 `(bool, value)` 契约,现兜底 `code=0` 并保留原文);**Keyence Host Link 分类口径**6 处"响应形状不符"由 `OmniPLCInternalError` 改抛 `ProtocolFrameError`(分类 UNKNOWN → PROTOCOL,与 `_transact` docstring 承诺一致,消息按坏帧诊断口径补原始数据);删除死代码 `keyence/address.py::offset_device`;aio `close()` docstring 补取消路径边界。(4)**测试与文档**:新增 211 例(861 → **1072**,零跳过),原生用例在 **Selector 与 Proactor 两种事件循环**上各跑一遍(3.7 的 Proactor 不支持数据报端点,必须双跑才能证明 UDP 选型正确),测试脚手架零新增依赖(用 `asyncio.run` 包同步测试,不引入 pytest-asyncio);README 新增「两套异步」选型表与原生命中层用法/边界,`architecture.md` 新增 §3 第 8 条与 **§12 原生异步层**(复用边界、超时/取消行为表、3.7 实测依据),§9 补"同步 × 异步对拍 + 双事件循环",§10 补 asyncio 可用面,`docs/real-machine-checklist.md` 标注原生层为独立代码路径待真机核证。**行为变更提示**:`write_bool` 拒绝非 0/1 的 int(此前按真值写入);原生层为新增能力,不影响既有同步/`omniplc.aio` 行为。门禁:pytest **1072 passed**(+ ruff / mypy / ty 全零)。
- **v0.38.0**:转换助手与类型面收口批——(1)**`convert.words_to_value` 支持 BOOL/STRING**:此前只认 8 个数值类型,`BOOL`/`STRING` 一律抛 `ValueError`,而 `read_bool`/`read_string` 家族与各驱动的位/文本读取本身就是**按字**取回,现场拿批量读出的寄存器还原成 Python 值时只能自己拼字节。现按类型分流——`BOOL` 要求 1 个字、按"**字值非 0 即 True**"解码(`byteorder` 不参与,单字只看数值),字数不符显式 `ValueError`;`STRING` **不限字数**(入参几个字就解几个字,0 个字得空串),按 `byteorder` 拼字节后走同一条 `decode_string`(`\x00` 截断),新增 `encoding` 参数(默认 ascii,与各 `read_string` 同口径)——**寄存器文本通常按大端存放,读文本请显式传 `ByteOrder.BIG`**(与 Modbus `_read_string` 的 `reg.to_bytes(2, "big")` 同源),该口径已写进 `:param byteorder:` 与 `:example:`;非 `DataType` 入参的兜底报错由"不支持的数值类型"改为"**不支持的数据类型**"(BOOL/STRING 已支持,原文案会误导)。反向的 `value_to_words` **维持只收数值类型**:编码必须由调用方给出目标长度(寄存器个数由设备侧约定),不对称的理由写进 docstring,报错文案改为"value_to_words 只支持数值类型"。(2)**`stats` 返回类型改 `ClientStats`(TypedDict)**:9 个字段与 `BaseClient.stats` docstring 字段表一一对应,包顶层(`omniplc.ClientStats`)与 `omniplc.core` 导出,异步镜像 `ABaseClient.stats` 同型,下游标注后即得字段补全与键名静态检查;3.7 无 `typing.TypedDict`,按 `sys.version_info >= (3, 8)` 分支导真类型、低版本退化为 `dict` 子类(类体只有注解),**不引入 `typing_extensions` 运行期依赖**,核心零依赖不变;两套检查器对 TypedDict 的收窄口径不一致(ty 不认 dict 字面量与 `TypedDict.copy()`,mypy 可以),故快照行用一处 `cast(ClientStats, ...)` 兼容(项目禁 `# type: ignore`,cast 是既有写法),并加测试锁"快照键集 == 声明字段"防今后新增计数漏声明。**运行期行为零变化**:返回的仍是普通 dict,且每次返回拷贝(改返回值不影响内部计数,既有隔离语义保留)。(3)**测试与文档**:+19 例(convert 16 例:BOOL 非 0 为真/字数校验/字节序无关,STRING 大端 ASCII/与 `encode_string` 互逆/默认小端差异/NUL 截断/空字序列/utf-8 多字节跨字/`reverse_words`;类型契约 5 例:键集一致/运行期仍是 dict/隔离拷贝/包顶层与 core 导出同一类型/异步镜像键集),README「连接健康统计」节标注返回类型并给下游标注示例,`architecture.md` §6.1(convert 入参口径)/ §6.2(TypedDict 条)/ §6.3(`stats` 签名)同步;`docs/review.md` 入库 v0.37.0 复审修订(§7.7 修复批次逐 diff 复核 + K1 待核项)。门禁:pytest **861 passed**(+ ruff / mypy / ty 全零)。
- **v0.37.0**:契约与口径收口批(review 复审 + 多角度自查)——(1)**超时与错误码口径统一**:`_execute` 新增 `TransportTimeoutError` 独立分支——接收超时(**0 字节已读 = 链路无残渣**)不拆连、不计 `device_error_count`(它不是设备返回的错误码)、但与其他传输失败一样按 `retries`/`write_retries` 重试,串口/UDP 与 TCP 的 `retries` 语义就此统一(此前串口/UDP 超时从不重试);`_extract_code` 把 `DeviceError(code=0)` 统一归 `None`(无具体错误码;有码照原样返回,含 UDP 报文超长的 `WSAEMSGSIZE` 10040);`device_error_count` 按字段文档收口为"**有码才计**"(能力缺失、设备侧条件这类无码失败只进 `error_count`);超时文本去掉类名前缀;`_categorize` 删去 `ConnectionRefused/Reset`、`gaierror` 冗余分支(均为 `OSError` 子类);`_execute` docstring 修正"所有内部异常都转换"的过度承诺(调用方/编程错误仍直接抛);(2)**SR 扫码枪事务语义统一**:`_read`/`_write` 能力缺失由 `OmniPLCInternalError` 改抛 `DeviceError`(调 `read_*`/`write_*` 不再拆线)、`scan()` 并入 `_execute`(新增事务计数与退避门控,`is_write=True` 取 `write_retries` 防重复触发激光)、`reset()` 同改;超时仍不拆线(先 drain 半行残留再抛 `TransportTimeoutError`);(3)**Modbus 批量写收口**(复审实测出 2 个 P1 + 4 处半成品):超限切片改为**并入前判定**(126 连续字 / 2001 连续线圈 / 64 连续 FLOAT 不再抛 `ValueError`、不再零字节失败)、写侧不再吞异常(`_write_and_apply` → `_write_chunk` 异常穿透,失败原因落 `last_error`;`write_batch` 整批失败、`write_many` **每 chunk / 每 RMW 项独立事务**且返回值不再含 `None`)、写事务补 `is_write=True`(此前写路径吃读重试,`retries=3` 时同一写命令重发 4 次)、地址跨度改**入参期**校验(32/64 位类型计入字宽,越界零字节发送,不再"先写掉合法项再抛");(4)**`write_bool` 值校验**:非 bool/int(如 `"0"`/`"false"`/`1.0`)显式 `ValueError`,此前被 `bool()` 吞成 `True` 静默写反;(5)**MC 4C 帧截断语义**:帧中途超时(已消费帧头与部分正文)由 `TransportTimeoutError` 改抛 `TransportClosedError`,按串口截断拆连重同步(此前按"0 字节已读"处理 → 不拆连且重试,残渣必致下一帧错位);(6)**松下 MC 批量地址换算修复**:补 `_translate_address` 覆写——此前 `read_many`/`read_batch` 绕过换算钩子,**按未线性化的编号静默访问别的软元件**(如 `R1003` 发 1003 而非 1603;单点读写正常,故最难察觉);(7)**aio 安全关闭与边界公示**:`close()` 改为"**关闸 → 排空 → `shutdown(wait=True)`**"(关闸后不再受理新调用、在途事务不被锯断、在默认执行器里等待不阻塞事件循环),此前 `wait=False` 期间新任务会排在 disconnect 之后 → **关闭返回后仍有帧落线**;README/architecture 明确异步层是"同步 I/O + 单线程池包装"而非原生 asyncio(同一客户端串行、同步属性直读非原子、**无原生取消**:`wait_for` 超时后已提交的写事务仍会跑完);(8)**测试与文档**:842 例 **0 跳过**(原先按平台互斥跳过的 3 例改为假 socket 跨平台验证,POSIX `MSG_TRUNC` 截断分支首次纳入覆盖),本轮新增约 30 例回归(其中 7 例在旧实现上实测 FAILED,确认为真回归);`CONTRIBUTING.md` 门禁用例数改为"随批次增长"不再承诺固定值;`docs/review.md` 入库并同步各缺陷状态与提交落点。**行为变更提示**:`device_error_count` 不再计无码失败与超时(看板数值会变);`write_many` 由"单事务整批"改为"每 chunk/RMW 项独立事务"(批内原子性不再,docstring 已声明);`write_bool` 拒绝非 bool/int 入参。门禁:pytest 842 / ruff / mypy / ty 全零。
- **v0.36.0**:Modbus 规范能力补齐 + 协议校验加固批——(1)**Modbus 原生批量读**:覆写 `read_many` 与新增 `read_batch`(混类型入参),按 `(区域, kind, width, 数据类型)` **5 类分组**、组内按偏移升序**相邻合并**(gap=0)为单条 FC 01/02/03/04;Modbus 协议不支持跨 FC 单事务,故为 **K 笔而非 1 笔**(K ≤ 地址数,典型 1 笔),跨区/跨类型/地址空洞各起一笔;超 FC 上限(位 2000/字 125)在连续区内二次切片;寄存器位(`hr0.3`)读走字区提位并入合并;失败语义对齐 MC/FINS(任一笔 FC 失败 → 整批失败),入参非法在组帧期同步 `ValueError`(零字节发送);(2)**Modbus FC 15/16 合并写**:与读侧对称的 `write_many`(保持基类 `List[bool]` 契约)与 `write_batch`(整批容错 `(bool, [bool])`),新增 `_write_bools_impl`(FC 15)补齐对称路径;寄存器位写**不入合并**(FC 16 写 1 字会冲掉其他位)走"读-改-写",同批次内按偏移升序先 RMW 后 FC 16(竞态与基类逐点行为一致,已文档化);(3)**Modbus FC 23 读写多寄存器**:`read_write_registers(read_address, read_count, write_address, values)` 单事务"先写后读"(写上限 121,较 FC 16 的 123 小 2),控制场景省一个往返且无中间态插入;仅保持寄存器、广播站号拒绝;(4)**Modbus FC 43/14 读设备标识**:`read_device_id(level)` 流式访问 1/2/3 且 **More Follows 自动翻页**,标准对象(0x00~0x06)映射为规范名键(`vendor_name`/`product_code`/`major_minor_revision`…)、厂商私有对象用 `object_0xNN`,值为 ASCII 文本(非法字节替换);`read_device_object(object_id)` 个体访问 4 返回原始字节;**RTU 走线按对象头增量收包**(响应长度随对象数变化无法按请求推算,`_recv_device_id_tail` 先读 6 字节固定头再逐个对象读"对象号+长度"与值),TCP 按 MBAP 长度域收包无需改动;响应解析校验 MEI 回显、对象头/值截断、尾部冗余字节;(5)**Modbus 起始地址+数量越界前置拒绝**:`_check_span` 应用于读(FC 01~04)与批量写(FC 15/16),越过地址空间顶端(如 `hr65535` 读 2 字)在组帧期 `ValueError`,不发必然被设备拒(异常码 02)的请求;(6)**FC 23/43·14 未做项按裁决保留**:ASCII 走线、文件记录 14/15、FIFO 18、串行诊断 07/08/0B/0C/11/17;**FINS 协议校验加固**:构造期路由字段范围校验(network/node 0~127、unit 0~255,消除 `& 0xFF` 静默截断)、节点号推导结果校验(IP 末段须 1~126,超限提示显式指定)、应答帧身份回显校验(ICF/SID/命令码三层,识别串话与迟到响应)、TCP 长度域下限(< 14 字节即坏帧);**MTConnect XML 纵深防御**:3.7.9 stdlib `ElementTree` 对 DOCTYPE 内部实体默认展开(实测坐实),`forbid_dtd`/`forbid_external` 为 3.8+ 才有,改为解析前扫描拒绝 `<!DOCTYPE`(MTConnect 合法负载不使用,零误伤)覆盖 XXE 与实体炸弹;**测试与文档**:Modbus 黄金样本 10 → **18**(FC 23 规范 §6.17 示例逐字节一致、FC 43 单页/翻页/异常,TCP+RTU 双走线,生成器独立实现同步扩展)、新增 `test_modbus_codec.py`;三项能力均含 aio 镜像(内省守卫通过);门禁 806 + 2 skipped / ruff / mypy / ty 全零。
- **v0.35.0**:OPC-UA 推模式补齐(docs/review.md P1 收口)——(1)**DataChange/Event 订阅**:`subscribe_data_change(node, on_change, sampling_interval_ms=1000)`(回调 `(value, node_id, source_timestamp)`,用户回调异常吞掉——log + `last_error` category=UNKNOWN,不杀订阅)/`subscribe_event(node, on_event, event_filter=None)`(EventFilter 可选透传);`OpcUaSubscription` 订阅句柄(`node_id`/`subscription_id` 只读属性,`unsubscribe()` 幂等——二次静默返回 False),`active_subscriptions` 活跃订阅快照(`subscription_id` → 句柄);`disconnect()` 重写为先退订清索引再走基类断开;**断线不自动重订**(订阅重建策略留调用端,与拉模式读取的惰性重连语义区分);(2)**地址树 Browse**:`browse(node_text="Root", recursive=True, max_depth=None)`,Root/Objects/Types/Views 语义别名,递归枚举 + `max_depth` 深度上限,返回 `{node_id: {browse_name, node_class, children}}` 嵌套字典;OPC-UA 语义:不存在节点返回空引用列表不报错(与 Read 报 BadNodeIdUnknown 不同);(3)均含 aio 镜像——回调经 `loop.call_soon_threadsafe` 桥接到事件循环线程(回调内可安全做 asyncio 操作,loop 已关闭静默丢弃),内省守卫通过;(4)13 例真实 asyncua 服务端测试(`asyncua.sync.Server` 为 module 级启停、端口在 import 期临时分配:Browse 5 例 / 订阅 3 例 / 生命周期 3 例 / aio 2 例);永久决议:TLS/X.509 安全栈内网部署不考虑;asyncua 版本钉 1.1.5 不变(3.7 最后支持线)。
- **真机联测记录(随 v0.35.0 发布)**:三菱 MX Component(三菱 FX3U)读/写通过;欧姆龙 FINS UDP(欧姆龙 CP1H)读/写通过。核验明细见 `docs/real-machine-checklist.md`(读/写独立勾选)。
- **v0.34.0**:可靠性 P0 双项——(1)**连接退避门控**:建连/握手失败后指数退避(full jitter,`uniform(0, min(0.5×2ⁿ, 30))` 秒,`monotonic()` 时间戳门控零 sleep),`reconnect_backoff` 可写属性(默认开,`retries` 同款)+ `next_connect_in` 只读属性;防 PLC 断电高频重连风暴,门控拒绝不计 `error_count`,连接成功/显式断开全重置;(2)**失败结构化**:`ErrorCategory` 五值枚举(TRANSPORT/PROTOCOL/DEVICE/TIMEOUT/UNKNOWN,TIMEOUT 独立——链路完好可重试,与坏帧区分)+ `last_error_category`/`last_error_code` 只读属性(`DeviceError.code` 协议原始码 / `OSError.errno` 直通),`last_error` 文本契约不变;失败写入统一收口 `_set_error`/`_clear_error`(SR 扫码枪 ×5、AB 解码 ×2 直写点迁移);aio 镜像全量转发,内省守卫通过。
- **v0.33.0**:MC A 兼容 1C 帧落地(`MelsecMcSerialClient(frame="1C")`,命令 BR/WR/BW/WW)——C24 A 兼容通信,ASCII 格式 4(ENQ 起始、和校验后接 CR LF),与 3C/4C 共享同一串口客户端;软元件规格 X/Y/B/W 十六进制、M/L/S/F/D/R 十进制,T/C 双性质映射(字=TN/CN、位读=TS/CS、位写=TC/CC),位软元件按字访问起始编号须 16 的倍数;点数上限 BR 256/BW 160/WR·WW 64 字(位软元件按字 32/10);消息等待(`message_wait`,0~15,10ms 单位)构造期可配;与 3C 的差异——无帧识别码、路由缩为站号+PC 号、错误代码 2 位、命令字 BR/WR/BW/WW(ACPU 共通),JR/QR/BT/WT/监视登录/扩展文件寄存器/TT 等与点位读写契约不符未做;以本地 SH-080008-AB 第 17 章逐字节核证(`codec_serial_a.py`),黄金向量测试 21 例(向量内联于用例,含 T/C 映射、点位上下限、回显/和校验/控制码坏帧、aio 镜像);`docs/architecture.md` MC 串口段补 1C 详解,协议覆盖表 RTU 列 3C/4C → 1C/3C/4C,backlog 行 1C/2C → 2C。**点位表 schema 破坏性变更**(随本期发布):`Tag.name` → `tag_id`(字母标识)+ 新增 `remark`(中文备注,可选);迁移指南见 README「点位表」节与 `tag.py` 文档。
- **v0.32.1**:CI 修复——Release 发布物收紧为显式 `dist/*.whl` + `dist/*.tar.gz`:`uv build` 会在 `dist/` 生成 `.gitignore`(内容 `*`,防构建产物污染 VCS,本地复现坐实),原 `dist/*` 通配把它一并挂上 Release(GitHub 存储名 `default.gitignore`、显示 `.gitignore`);收紧后发布物仅可能为 wheel + sdist,aio 产物清单同步收紧
- **v0.32.0**:API 对齐批——(1)**FINS 节点号自动推导**:`destination_node`/`source_node`(TCP 另含 `local_node`)缺省 `None` 自动获取——UDP 从 IP 末段推导(目标 = PLC IP 末段,源 = 本机出口 IP 末段,UDP connect 探测取同一路由出口),TCP 经握手获取;显式传值原样使用(OmronFins UDP 读 0x2108 真机排查的配套改进);(2)**参数归位四分法成文**(architecture.md §2.1:构造 = 身份冻结/可写属性 = 行为调优/`configure_serial` = 物理链路/只读 = 运行态;判定测试"改了它是不是等于换了一个对端")并补齐只读属性:FINS 路由六参、MC `network_number`/`pc_number`、MC 串口 `self_station_number`/`module_station`(含 aio 镜像,内省守卫通过);(3)**双入口一律取消**:Modbus `station` 与 SR `scan_dwell` 属性转只读(校验移到构造期),全库不变量 = 构造参数一律冻结、可写属性一律不进构造(仅剩超时/重试/字序五件);(4)**S7 构造签名对齐**:`(ip, rack, slot, port, dll_path)` → `(ip, port, rack, slot, dll_path)`(破坏性:位置参数调用需调整,关键字调用不受影响)
- **v0.31.4**:CI 修复批——`uv build` 显式指定 `--python 3.12`:仓库 `.python-version` 钉 3.7.9(本地开发用),GitHub runner 无此解释器且 uv 托管下载不支持 3.7(首跑报 "No interpreter found for Python 3.7.9");构建本身与运行版本无关(hatchling 纯 Python,产物 wheel 为 `py3-none-any`),setup-uv 预装 3.12 后构建恢复正常
- **v0.31.3**:CI 发布流水线——新增 GitHub Actions 工作流(`.github/workflows/build-release.yml`),打标签推送(`v*`)自动触发:`uv build` 构建 sdist + wheel、挂到对应 GitHub Release,并经**可信发布(OIDC)**发布到 PyPI(工作流内无明文令牌;`fork` 守卫防误发;Gitee Go 流水线为企业付费服务,个人账号不可用故未配置)
- **v0.31.2**:文档完善批——(1)**全库客户端类构造参数注释补全**:AST 审计 43 处缺口全部补齐(aio 异步包装 29 处"参数同 X"引用式改为显式 `:param`、同步客户端 9 处委托式改为显式参数块——`OmronFinsTcpClient` 补漏注的 6 个 FINS 路由字段、内部会话适配器 5 处补全),措辞与同步侧逐字一致防漂移;(2)**README 同步修正**:类继承图补 `MTConnectClient`/`SiemensS7Client` 节点与箭头、异步镜像清单补 `AMTConnectClient`/`ASiemensS7Client`、安装节补 `[ads]`/`[s7]` extra;(3)**使用范例修正**:`read_many` 返回逐点列表的语义错误解包修复(`ok, values = ...` 改为 `results = ...`),全部 7 个代码块经 AST+真实签名审计零问题;(4)路线图按版本号降序重排(新版本在前)
- **v0.31.1**:MX Component 块读写原始 vtable 调用补第 4 参——`ReadDeviceBlock`/`WriteDeviceBlock`/`ReadDeviceRandom`/`WriteDeviceRandom` 的 raw 调用须按 vtable 全参收,尾参 `lplRetCode`(通信函数的返回值,[out,retval])也要自备 `ctypes.c_long` 缓冲传入(3 参调用真机报 "this function takes 4 arguments (3 given)",实机两轮联测核证);方法返回值为 COM 层 HRESULT,业务出错码以尾缓冲内容为准,新增 `_check_com_call` 双校验(HRESULT 失败 = COM 层问题,通信函数返回值非 0 = PLC 出错代码);`this` 由 comtypes 绑定自动提供、不占参数位——报错文案判别:raw 通道 "this function takes N (M given)" 的 N 是业务参数个数、高层包装 "call takes exactly N" 的 N 含 self;`get_cpu_type`/`get_clock` 高层双形态路径实机确认正常
- **v0.31.0**:MX Component 真机联测批(真机两轮联测核证 COM 出参口径 + 查询能力扩充)——(1)**comtypes 出参约定逐方法修正**:`GetDevice` 出参收进返回值,单参调用直接返回数据(传 byref 缓冲报参数个数 TypeError);`ReadDeviceBlock`/`ReadDeviceRandom` 数组参数以 ctypes 缓冲传入原地填充(省略缓冲调用不报错但返回值是出错代码、数据被丢弃,症状 `int object is not iterable`),返回码照常校验;`SetDevice`/`WriteDeviceBlock` 纯入参;GetDevice 路径失败以 COMError 形态出现,翻译为 `OmniPLCInternalError` 走断线重连;(2)**新增方法**:`write_batch` 随机批量写(WriteDeviceRandom;位 + 16 位字混合单事务,拒绝位后缀挂字软元件、32 位类型、超 `MX_MAX_BLOCK_WORDS`)、`get_cpu_type`(GetCpuType)、`get_clock`/`set_clock`(Get·SetClockData,字段序 年/月/日/星期/时/分/秒)、`get_error_message`(GetErrorMessage,独立 ActSupportMsg 控件,ProgID 按命名惯例推定待真机核证);未核证方法防御性双形态(短参优先,TypeError 回退 byref VARIANT);均含 aio 异步镜像;(3)**类型化读→MX 方法映射**:`GetDevice` 单点=1 字 16 位,`read_int/uint/float` 走 ReadDeviceBlock 2 字(低字+高字×65536 小端拼合,INT 再转补码)、`read_long/ulong/double` 走 4 字、`read_string` 走 ⌈n/2⌉ 字拼字节;"2" 系列是 SHORT 型数据版而非 32 位
- **v0.30.2**:安全修复(2026-09-23 三路安全审计收口)——(1)**网络长度域 recv 前上限快失败**:FINS/TCP 32 位长度域 ≤8192(握手与数据路径共用)、MC 3E/4E 应答数据长 ≤8192、MC 4C 串口长度域 ≤4096、TOYOPUC 帧长 ≤2048、AB ENIP 长度域 ≤8192,超限按坏帧断线——恶意设备/中间人声明的超长收包不再能导致内存耗尽或事务阻塞(原 FINS `0xFFFFFFFF` 长度域可灌 350MB+);(2)**逐字节收包改整事务 deadline**:MC 4C / KV Host Link / SR 扫码枪 / OpenTcp 的收包循环一次性起算 deadline、每轮下发剩余预算,滴流对端不再能逐字节重置超时长占事务锁(原 4C 最长 ~54 小时、KV ~3.4 小时),各协议超时/断线语义不变;(3)**MTConnect 响应体上限**:分块读取 + 16MB 上限(超限坏帧断线),数值文本 >64 字符快拒(py3.11 前 `int()/float()` 对超长数字串超线性);补 9 例安全回归测试
- **v0.30.1**:修复合集——(1)MTConnect keep-alive 失效原位重建透明重试(Agent 空闲超时静默关连接后,下一次 GET 原位重建重试一次,轮询不再周期性断线;`urllib` 已裁决不采用:`urllib.request` 显式 `Connection: close` 每请求新建 TCP,轮询场景是退步);(2)点位表 `write_tag` 整数点位逆缩放还原 int(真除法恒产生 float,被底层整数校验拒收,整数点位写入必挂——默认 scale=1.0 也挂),`scale=0` 建表期拒绝,JSON 元素非对象/CSV 短行等导入异常契约收口;(3)OpenTcp 流式成帧改用 `recv_some`(修复真服务端短帧永远收不到——`TcpTransport.recv` 的"读满恰好 size"契约被当流式读误用,短帧凑不满卡到超时);(4)全协议收包语义收口:MC 1E 读错误响应按结束码分流(1E 帧无长度域,错误响应只有 2 字节头,原实现按成功长度继续收数据,阻塞到超时误判断线)、SR 扫码枪超时残留尽力清理防串帧(读超时不断线语义保留)、KV UDP 整包上限 2048→4096 对齐流式上限;测试基建新增 size 感知假 socket(`scripted.ChunkSocket` + `mount_real_tcp`),MC 1E/3E、Modbus、AB 真传输凑满语义回归
- **v0.30.0**:工业场景可靠性与观测诊断收口——(A1)`receive_timeout` setter 下发到 live socket(TCP/UDP/串口);(A2)整事务 deadline 防涓流拖死(TCP/串口按 `monotonic()` 绝对 deadline 重设 `settimeout`);(A3)新增 `TransportTimeoutError(DeviceError)`,串口/UDP 超时按链路完好不断线,TCP 仍 OSError 拆连防串帧;(A4)`connect()`/`_after_connect()` 异常统一清理到干净状态(失败必 close+null+返回 False);(A5)AB connected 模式 CIP 状态 0x01(`Connection failure`)映射为 `ProtocolFrameError`,断开惰性重连重建 Forward Open;其余 CIP 状态(只读等)保持 DeviceError 不拆连;(A6)TCP 默认启用 SO_KEEPALIVE,Linux `TCP_KEEPIDLE/INTVL/CNT`(30s/5s/3 次),Windows `SIO_KEEPALIVE_VALS`,best-effort 静默降级;(A7)aio `close()` 幂等——先尽力断开同步客户端再 shutdown executor,关闭后协议调用抛 `RuntimeError`;(A8)UDP datagram 上限 2048→8192(`MC_MAX_DATAGRAM`/`FINS_MAX_DATAGRAM`);(A9)FINS/TCP 重连刷新自动节点号(构造期 `auto_*` 标志保留,握手结果在自动模式下每次覆盖);(A10)MX COM `Close()` 先于 `_com` 清空,失败也清引用,`CoUninitialize` 配对释放线程计数;(B1)`BaseClient.stats` 健康统计(connect/disconnect/transactions/error/device_error 计数 + last_error_at/last_connect_at/last_success_at/last_rtt 时间戳),aio 镜像转发,README 字段说明 + 真机联测待做汇总清单
- **v0.29.2**:使用范例完善——逐客户端补读写与驱动特有扩展示例(MC/FINS/KV HostLink/MX 补完整读写,MX 补 ReadDeviceRandom 批量,AB 补 0x0A 批量与 Identity 服务),新增"批量读取(协议原生,单事务)"专节(五家原生能力与统一契约),异步节补多设备并发示例与性能提示(并发 ≈ 顺序 1/N 耗时)
- **v0.29.1**:异步镜像完整性收口——全量内省审计补齐 9 处缺口(基恩士 MC ×2/汇川 MC/松下 MC 的 `read_batch` 经对称继承获得,AB/NJ 补 `generic_message`/`list_identity`/`get_plc_info`/`get_attribute_all`/`get_attribute_list`,NJ 补 `slot`,汇川 TCP/RTU 补 `station`/`word_order`/`write_mask_register`);aio 家族镜像改对称继承结构(Keyence/Inovance/Panasonic MC 继承 AMelsecMc*,汇川 Modbus 继承 AModbusBaseClient);新增内省守卫测试:同步扩展必须有异步镜像,防再漂移
- **v0.29**:MX Component 批量读取——ActUtlType 原生 `ReadDeviceRandom`(`read_batch` 混软元件单事务、`read_many` 覆写;软元件列表换行分隔每条 1 字;仅 16 位类型 BOOL/SHORT/USHORT,32/64 位因地址编号原文透传无法安全拆字,逐点读取;手册 5.2.5 核证)
- **v0.28**:CIP 与 OPC-UA 批量读取——AB 0x0A 多服务包(`read_batch` 混标签混类型单事务,BOOL 首次类型发现后入包,NJ/NX CIP 零改动继承;按 CIP 规范逐条核证,内嵌服务 32 条上限;通用模拟器不支持 0x0A,真机核证待做)+ OPC-UA UA Read 服务原生多节点(asyncua `read_values` 单请求,`read_many` 覆写)
- **v0.27**:欧姆龙 FINS 批量读取,利用协议原生 0104 多存储区读(`read_many` 覆写为单事务、`read_batch` 混类型混软元件;W342 §5-3-5 核证:每条读 1 字、响应逐条区码回显、仅字码,以太网上限 167 条;BOOL 走包含字提位,T/C 完成标志不支持)
- **v0.26**:MC 3E/4E 批量读取,利用协议原生 0406 多块批量读(`read_many` 覆写为单事务、`read_batch` 混类型混软元件;SH-080008 §8.4 逐字节核证,总块数 ≤120;汇川/基恩士 MC 兼容子类经地址换算钩子继承;1E/3C/4C 回退逐点/拒绝)
- **v0.25**:OpenTcpClient 补定长成帧(`frame_length`,二进制固定帧设备;与 `delimiter` 互斥、构造期二选一校验,`frame_length` ≤ `max_frame`,`append_delimiter` 强制关;跨分片/多帧/残字节语义与分隔符模式一致,异步镜像同步)
- **v0.24.1**:S7 依赖按 Python 版本自动二选一(3.7~3.9 → python-snap7 1.3,extra 带 setuptools 修 pkg_resources;3.10+ → 3.x 纯 Python 无需 DLL),并修复区码需转 snap7 `Areas` 枚举的兼容问题(1.x 裸 int 读抛 ValueError/写抛 AttributeError);错误边界适配 3.x `S7Error` 谱系
- **v0.24**:西门子 S7(封装 python-snap7,rack/slot 路由 102;DB/I/Q/M 绝对寻址,尺寸由 DataType 决定大端序,位读改写,S7 String;64 位 Python 用捆绑 snap7 库,32 位经 dll_path 自备)
- **v0.23**:CNC 机床数采 MTConnect(标准库 HTTP/XML 只读,Agent 默认 5000;数据项 id 即地址,类型化读 + 全量快照 + 报警条件项 + 设备信息;FANUC/三菱等控制器均可经 Agent 采集,零第三方依赖)
- **v0.22**:内部性能与整洁度优化(全驱动地址解析 lru_cache 缓存,MC 事务提速约 12%;会话型调试日志惰性格式化;check_byte_field 归位 core/validation)
- **v0.21**:全局报文调试开关(omniplc.set_debug;走线型在传输层统一输出请求/响应十六进制,会话型 OPC-UA/ADS/MX 输出操作级日志;logging 记录器 omniplc.debug,无日志配置时自动落 stderr)
- **v0.20**:倍福 TwinCAT ADS(封装 pyads 3.5.1,AMS 端口 851;变量名读写,DataType→PLCTYPE 映射,ADSError→DeviceError 不断线;pyads 无 py37 语法障碍,Windows 需 TcAdsDll)
- **v0.19**:通用自定义 TCP 客户端(OpenTcpClient;分隔符成帧 + 内部缓冲,重连/超时沿用 BaseClient 属性,receive/transact 支持 per-call timeout;超时不断线,坏帧/解码失败断线惰性重连,重连清空接收缓冲)
- **v0.18**:欧姆龙 CIP / 连接型 CIP(NJ/NX 内置 EtherNet/IP,44818;继承 AB 客户端,unconnected 直发无背板路由 + connected 连接路径只剩消息路由对象,NJ 变量读写;按 CIP/EtherNet/IP 规范核证)
- **v0.17**:AB connected CIP 消息(Forward Open 大/普通回落 + SendUnitData 序列号回显校验 + Forward Close,connected_messaging 参数启用)
- **v0.16**:罗克韦尔 AB EtherNet/IP(CIP,44818;Logix 标签读写,unconnected 消息 + 槽号路由,标签自描述,位/BOOL 数组原子读-改-写,STRING 结构体)
- **v0.15**:基恩士 KV MC 协议兼容 UDP 走线(继承 MelsecMcUdpClient,与 TCP 版共用码表覆写,端口 5000)
- **v0.14**:三菱 MC 串口帧(C24;3C 帧 ASCII 格式 4 / 4C 帧二进制格式 5,帧格式按 SH-080008 Appendix 7 逐字节核证)
- **v0.13**:松下 FP0H/FP7 MC 协议兼容(3E 帧)+ MEWTOCOL(TCP/UDP,1024;RCS/WCS/RD/WD,BCC 校验)
- **v0.12**:汇川 MC 协议兼容(3E 帧,继承 MelsecMcTcpClient;S→L 码、R=D+8000 统一编址、X/Y 八进制换算)
- **v0.11**:汇川 H3U/H5U(Modbus TCP/RTU,继承 Modbus 客户端换汇川地址映射)
- **v0.10**:基恩士 KV MC 协议兼容(SLMP 3E 帧,继承 MelsecMcTcpClient 换码表)
- **v0.9**:Modbus 协议对照强化(对照 Modbus 规范:RTU 广播写、FC22 掩码写、MBAP 长度上限)
- **v0.8**:OPC-UA opc.tcp 会话(封装 asyncua 1.1.5,NodeId 读写)
- **v0.7**:丰田 TOYOPUC 计算机链接(TCP/UDP,基础区字/字节/位访问)
- **v0.6**:基恩士 SR 扫码枪(LON/LOFF 触发扫码,bank 预设)
- **v0.5**:基恩士 KV Host Link(TCP/UDP,RD/RDS/WR/WRS)
- **v0.4**:三菱 MX Component(comtypes,逻辑站号)+ MC float 解码修正
- **v0.3**:三菱 MC 3E/4E/1E(TCP/UDP)+ 欧姆龙 FINS TCP/UDP(握手/节点分配)
- **v0.2**:Modbus TCP/RTU 编解码 + 黄金报文样本 + 脚本化链路测试
- **v0.1**:架构落地 + 公共层/传输层完整实现 + 协议客户端骨架 + 100 例测试