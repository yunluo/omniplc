# 安全漏洞披露策略

## 支持的版本

| 版本 | 支持状态 |
|---|---|
| 最新发布版本(`master` 分支) | ✅ 接收漏洞报告 |
| 前一发布版本 | ✅ 接收漏洞报告 |
| 更早版本 | ❌ 不接收,请先升级 |

## 报告流程

**请勿在公开 issue / PR / 讨论区披露安全漏洞**——给攻击者可乘之机。
请通过以下私密渠道之一直接联系项目维护者:

- **Gitee 私信**:yunluo(`https://gitee.com/yunluo`)
- **GitHub 私信**:yunluo(`https://github.com/yunluo`)
- 报告触发的事件将作为私有 issue 跟踪,**不在公开 timeline 暴露细节**

## 报告应包含的信息

请尽量提供以下信息(便于快速验证与修复):

- 受影响版本(`pip show omniplc` 输出 / git commit hash)
- 漏洞类型(协议注入 / 拒绝服务 / 凭据泄露 / 越权访问 / ...)
- 复现步骤与最小代码示例
- 漏洞的可观察影响(日志、内存、连接状态)
- 是否已在生产环境复现,影响范围

## 响应承诺

- **首响应**:48 小时内确认收到
- **修复节奏**:严重漏洞(critical)在 7 天内出 patch;其他在下个常规版本
- **致谢**:修复发布后,在 release note 致谢(若报告者同意);不公开未授权报告者身份

## 已知安全基线

本仓库已在 v0.30.2 收口一轮安全审计:

- 网络长度域 `recv` 前上限快失败(FINS/MC/AB/TOYOPUC 等)
- 整事务 deadline 防涓流拖死(MC 4C / KV / SR / OpenTcp)
- MTConnect 响应体上限(16MB)与数值文本长度快拒
- `OMNIPLC_INTERNAL_ERROR` 链全在控制流,公共 API 不抛(降低信息泄露)

详见 `CHANGELOG.md` v0.30.2 条目。