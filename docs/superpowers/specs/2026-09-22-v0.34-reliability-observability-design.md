# v0.34.0 重连退避 + 错误结构化 — 设计文档

**版本定位**:v0.34.0(minor 升级,纯新增 + 默认更安全,无 breaking change)
**设计日期**:2026-09-22(r2:2026-09-24 自审改判;r3:2026-09-24 写前原值取消,见 §7 #12)
**状态**:已批准,等待实施
**目标**:回应 `docs/review.md` 两条 P0(重连退避 + 失败结构化)

---

## 一、范围与动机

### 1.1 改动清单

| 项 | 来源 | 优先级 |
|---|---|---|
| **P0-1 重连退避**(exponential backoff + full jitter,时间戳门控,`reconnect_backoff` 可写属性默认开) | `docs/review.md:27, 176` P0 | **P0** |
| **P0-2 错误结构化**(`last_error_category` 枚举 + `last_error_code` 数字) | `docs/review.md:46, 133, 177` P0 | **P0** |

**已取消**:写前原值采集(原 P0-3,`read_before_write`)——2026-09-24 用户裁决移出 v0.34,见 §6 / §7 #12。r2 设计稿留在 git 历史(b89e7a2),重启时可直接复用。

### 1.2 动机

- `docs/review.md` 已点明:连接风暴场景下当前是"一次失败立即重连"模式,PLC 断电/网线松动会触发高频连接请求;`last_error` 是 str,上位系统做告警分类只能 `str.contains`。

### 1.3 改动文件(预估)

| 文件 | 性质 | 估行数 |
|---|---|---|
| `src/omniplc/core/base_client.py` | 改 | +120 |
| `src/omniplc/core/errors.py` | 改 | +30 |
| `src/omniplc/core/__init__.py` | 改 | +3 |
| `src/omniplc/aio/__init__.py` | 改 | +25(property + setter 转发) |
| `tests/unit/test_v034_reliability.py` | 新 | +200(退避 + 错误结构化主体测试) |
| `tests/unit/test_base_client.py` | 改 | +30(衔接既有 scripted 传输回归) |
| `tests/unit/test_aio_mirror_surface.py` | 改 | +25(镜像守卫扩充) |
| `docs/architecture.md` | 改 | +55 |
| `README.md` | 改 | +40(含路线图) |
| `pyproject.toml` | 改 | 1 |
| `src/omniplc/__init__.py` | 改 | 1 |
| `uv.lock` | 自动 | — |
| 总计 | | ~+530 |

**不动的**:所有具体客户端构造函数(`ModbusTcpClient`/`MelsecMcTcpClient`/… 约 25 个 `__init__`)零改动——新开关走基类属性,与 `retries`/`write_retries` 同款,无构造参数穿透。

---

## 二、P0-1 重连退避

### 2.1 算法

**Exponential + full jitter**(AWS 经典):
```
delay_n = uniform(0, min(base * factor^n, max))
```
- `base = 0.5` 秒,`factor = 2.0`,`max = 30.0` 秒
- `_connect_fail_count` 单调递增,`n` 取该值;连接成功后重置 0
- jitter 用 `random.uniform`,与库现有依赖一致(标准库)

### 2.2 物理形态:时间戳门控(零 sleep)

**守门位置:`connect()` 入口**(`base_client.py:106`)。公开调用与 `_execute` 惰性重连(锁内)走同一入口,一处守门两路径全覆盖。零 sleep 设计使锁内守门仅是一次时间戳比较,无阻塞代价。

```python
class BaseClient:
    def connect(self) -> bool:
        with self._lock:
            now = time.monotonic()
            if self._reconnect_backoff and now < self._next_connect_at:
                delay = self._next_connect_at - now
                self._set_error("连接退避中:%.1f 秒后允许重连" % delay,
                                ErrorCategory.TRANSPORT, None)
                return False          # 与现有 connect 失败契约一致:置 last_error 返回
            try:
                self._transport.connect(...)
                self._after_connect()
                self._connected = True
                self._next_connect_at = 0.0   # 成功 → 门控与计数全重置
                self._connect_fail_count = 0
                self._clear_error()
                return True
            except Exception as exc:          # 连接序列任何异常一律进入退避
                attempt = self._connect_fail_count
                cap = min(0.5 * (2.0 ** attempt), 30.0)
                self._next_connect_at = time.monotonic() + random.uniform(0.0, cap)
                self._connect_fail_count += 1
                self._set_error(_describe(exc), _categorize(exc), _extract_code(exc))
                return False
```

**关键设计**:
- **零 sleep**:不阻塞调用线程、不放大 RLock 持锁时间、不占用 aio executor 线程
- **失败契约与现状一致**:connect 失败置 `last_error` 三件套返回 False,读/写照现有惰性路径返回 `(False, None)` / False
- **单调时钟**:`time.monotonic()`,不受系统时间调整影响
- **运行中可关**:`client.reconnect_backoff = False` 即时跳过门控(每次连接时活读标志)

### 2.3 API 表面(与 retries/write_retries 同款)

```python
class BaseClient:
    def __init__(self, ip_address: str = "", port: int = 0) -> None:
        ...
        self._reconnect_backoff: bool = True       # 默认开
        self._next_connect_at: float = 0.0         # monotonic 时间戳
        self._connect_fail_count: int = 0

    @property
    def reconnect_backoff(self) -> bool:
        """连接失败后的指数退避门控(默认开)。断电/网线松动时防止高频重连风暴。"""
        ...

    @reconnect_backoff.setter
    def reconnect_backoff(self, enabled: bool) -> None:
        ...  # bool 校验,同 retries.setter 风格

    @property
    def next_connect_in(self) -> Optional[float]:
        """距下次允许 connect 的剩余秒数;None = 无门控,可立即连接。"""
        # 门控未启用或 _next_connect_at <= now → None;否则剩余 >0 秒
```

### 2.4 状态生命周期

| 事件 | `_next_connect_at` | `_connect_fail_count` |
|---|---|---|
| 初始化 | `0.0` | `0` |
| `connect()` 成功 | `0.0` | `0`(全重置) |
| `connect()` 失败(连接序列任何异常,含 `_after_connect` 握手失败) | `now + uniform(0, cap)` | `+1` |
| 门控命中(未到允许时间) | 不变 | 不变 |
| `disconnect()` / `__exit__()` | `0.0` | `0`(全重置) |
| 运行中置 `reconnect_backoff = False` | 字段保留但不参与判定 | 保留 |

### 2.5 aio 镜像

`ABaseClient` 增加 property + setter 转发(与 `retries` 转发同款,`aio/__init__.py:239-254` 风格):
```python
@property
def reconnect_backoff(self) -> bool: return self._sync.reconnect_backoff
@reconnect_backoff.setter
def reconnect_backoff(self, enabled: bool) -> None: self._sync.reconnect_backoff = enabled
@property
def next_connect_in(self) -> Optional[float]: return self._sync.next_connect_in
```
`_run(self._sync.connect)` 经门控自然短路,协议代码零改动。

---

## 三、P0-2 错误结构化

### 3.1 ErrorCategory 枚举

**新增**(`src/omniplc/core/errors.py`):
```python
class ErrorCategory(enum.Enum):
    TRANSPORT = "transport"   # OSError(ConnectionRefused/Reset/DNS 等)、TransportClosedError
    PROTOCOL = "protocol"     # ProtocolFrameError(坏帧/校验错/格式不符)
    DEVICE = "device"         # DeviceError(PLC 自身报错,code 为协议原始码)
    TIMEOUT = "timeout"       # socket.timeout、TransportTimeoutError(链路完好,不断线)
    UNKNOWN = "unknown"       # 其他(含裸 OmniPLCInternalError 兜底)
```

### 3.2 分类映射(单一规则表,顺序敏感)

```python
# 注意顺序:TransportTimeoutError 是 DeviceError 子类,必须先判
_CATEGORIZE = [
    (TransportTimeoutError,          ErrorCategory.TIMEOUT),
    (socket.timeout,                 ErrorCategory.TIMEOUT),
    (ProtocolFrameError,             ErrorCategory.PROTOCOL),
    (DeviceError,                    ErrorCategory.DEVICE),
    (ConnectionRefusedError,         ErrorCategory.TRANSPORT),
    (ConnectionResetError,           ErrorCategory.TRANSPORT),
    (socket.gaierror,                ErrorCategory.TRANSPORT),
    (OSError,                        ErrorCategory.TRANSPORT),   # 其余网络错误兜底
    (TransportClosedError,           ErrorCategory.TRANSPORT),
]
# 兜底:未命中(含裸 OmniPLCInternalError、意外异常)→ UNKNOWN
```

`_categorize_exception(exc) -> ErrorCategory` 助手按表 `isinstance` 短路;**新增异常类型时必须同步维护本表**(见 §8 风险表)。

### 3.3 code 提取

```python
def _extract_code(exc: BaseException) -> Optional[int]:
    if isinstance(exc, DeviceError):
        return exc.code          # MC 结束码 / FINS 结束码 / Modbus 异常码等
    if isinstance(exc, OSError):
        return exc.errno
    return None                  # ProtocolFrameError / UNKNOWN / 成功 → None
```

### 3.4 新增 property

```python
class BaseClient:
    @property
    def last_error_category(self) -> Optional[ErrorCategory]: ...
    @property
    def last_error_code(self) -> Optional[int]: ...
```

### 3.5 写入点

统一经 `_set_error(msg, category, code)` / `_clear_error()` 内部助手(加锁,与现有 `_last_error` 同步写),替换现有零散的 `self._last_error = ...` 赋值点:

| 位置 | 写入内容 |
|---|---|
| `connect()` 失败(含门控命中) | msg + category + code |
| `connect()` 成功 | 三字段全清 None |
| `_after_connect()` 失败 | msg + category + code |
| `disconnect()` 失败 | msg + category + code |
| `_execute` 失败 `DeviceError` | category=DEVICE,code=exc.code |
| `_execute` 失败 `OSError/Internal` | 按 §3.2 表分类 |
| `_execute` 事务成功(读或写) | 三字段全清 None(与现有 `_last_error` 清空同步) |
| `_mark_disconnected()` | 不设(由 `_execute` 设过) |

**`last_error: Optional[str]` 契约不变**——旧调用方零感知;两个新 property 是纯增量。

### 3.6 aio 镜像

两个新 property 转发到 `self._sync`(只读,无 setter)。

### 3.7 README 示例

```python
from omniplc import ErrorCategory

ok, val = client.read_ushort("D100")
if not ok:
    cat = client.last_error_category
    code = client.last_error_code
    if cat is ErrorCategory.DEVICE and code == 0xC059:
        alert.warn("MC 地址越界")
    elif cat is ErrorCategory.TRANSPORT:
        alert.crit("PLC 不可达")
    elif cat is ErrorCategory.PROTOCOL:
        alert.error("报文解析失败,检查链路噪声")
    elif cat is ErrorCategory.TIMEOUT:
        alert.warn("超时,可能对端负载高")
```

---

## 四、跨切面

### 4.1 测试

| 文件 | 类别 | 数量 |
|---|---|---|
| `tests/unit/test_v034_reliability.py`(新) | 退避 + 错误结构化主体 | 14-18 |
| `tests/unit/test_base_client.py` | 既有 scripted 传输衔接回归 | 2-3 |
| `tests/unit/test_aio_mirror_surface.py` | 三个新公共表面镜像守卫 | 1(扩充) |

**关键测试向量**:
- **退避**:连续失败 `_next_connect_at` 单调推进且 ≤30s;门控命中时 connect 返回 False 且 `last_error` 含"退避";成功后门控与计数全重置;`disconnect()` 后可立即连;`next_connect_in` 语义(None / >0);`reconnect_backoff=False` 行为等同 v0.33.0(既有 `TestLazyReconnect` 全数保持通过)
- **错误结构化**:§3.2 表逐行触发(含 TransportTimeoutError 先于 DeviceError 的短路序);DeviceError 取 code、OSError 取 errno、其余 None;成功清零;`last_error` 字符串与旧格式逐字节一致(向后兼容断言)
- **aio 镜像**:`reconnect_backoff` 的 setter、`next_connect_in`/`last_error_category`/`last_error_code` 的 getter 全部在内省守卫名单中

### 4.2 文档

- `docs/architecture.md`:
  - §2.1"入口与返回风格"补"运行态观察与调优属性"分节(`reconnect_backoff`/`next_connect_in`/`last_error_category`/`last_error_code`,与 retries 同族)
  - §4"连接状态机"加门控分支(mermaid 图补"退避中"状态)
  - §5"错误体系"加 ErrorCategory 表 + 映射规则
  - §10 状态链至 v0.34.0 + 履历表加行
- `README.md`:
  - "错误处理"章节扩写分类示例(§3.7)
  - 路线图加 v0.34.0 条目
- 文档自审:无 TBD、无相互矛盾、scope 聚焦

### 4.3 提交与版本

- 单 commit `v0.34.0: 重连退避 + 错误结构化`
- 五落点同步:
  1. `pyproject.toml` version 0.33.0 → 0.34.0
  2. `src/omniplc/__init__.py` `__version__` 同步
  3. `README.md` 路线图加 v0.34.0
  4. `docs/architecture.md` 状态链 + 履历表
  5. `uv.lock`(`uv lock` 自动)
- 推送双远程(gitee + github,用户单独确认)
- 门禁三件套:pytest 全量 / ruff / mypy

### 4.4 不破坏承诺

- 所有新公共表面:一个带校验 setter 的可写属性(`reconnect_backoff`,retries 同款)+ 三个只读 property(`next_connect_in`/`last_error_category`/`last_error_code`)——**无构造签名变更**
- 现有 `last_error: str` 内容与格式不变
- 现有 `is_connected` / `stats` / `retries` / `write_retries` 行为零变化
- `ErrorCategory` 为纯新增枚举,不进任何现有签名
- 无模块重命名、无方法签名变化

---

## 五、明确不做(用户裁决)

| 不做项 | 原因 |
|---|---|
| **写前原值采集本体**(`read_before_write` + `last_write_previous`) | **2026-09-24 用户取消**,移出 v0.34;r2 设计稿留 git 历史(b89e7a2)可复用 |
| `read_before_write` 的每次调用参数版(`write_*(..., read_before=True)`) | 随本体取消 |
| `client.transaction()` 上下文管理器 | 所有 write 隐式多读线,违反尊重现场实时性;read+write 不原子 |
| `write_history` 环形历史 | 若重启采集,快照单槽 + 调用方自存 |
| `compare_and_swap` 协议级 CAS 扩充 | 留 backlog,本版不动 |
| `stats` 加"上次错误至今"字段 | stats 已有 timestamps,够用 |
| 异步原生 asyncio 重写 | 架构改造,留 v1.x |
| ConnectionRefused/Reset/Timeout 细分到 `last_error_category` | 用户裁决 5 值粒度够用(细分在 TRANSPORT 内部,README 可用 `errno` 补充) |
| `last_error_history` 错误环 | 库只暴露 latest |
| 熔断器(连续 N 次失败短时拒绝) | 与退避功能重叠,退避已控风暴;留 backlog |

---

## 六、决策日志(对话归档)

| # | 决策点 | 选择 | 否决方案 |
|---|---|---|---|
| 1 | 退避作用域 | **仅连接失败后** | 事务重试间 / 连接+事务双层 |
| 2 | 退避参数暴露 | **可写 bool 属性,默认开(r2 改判,见 #11)** | 构造参数(不可达)/ 元组 / 类属性 / 硬编码 |
| 3 | 退避物理形态 | **时间戳门控(零 sleep)** | 真 sleep(阻塞 RLock/executor)/ 后台任务 |
| 4 | 错误结构化暴露 | **category + code 双字段** | 仅扁平错误码枚举 / 仅分类 |
| 5 | ErrorCategory 粒度 | **5 值,TIMEOUT 独立** | 4 值(TIMEOUT 并入 TRANSPORT)/ 10+ 细分 |
| 6 | 写前原值放置 | 讨论后用户**否决纯调用端 recipe**,选库内属性开关(后被 #12 取消) | 调用端 recipe / transaction() / 参数版 |
| 7 | 开关命名 | ~~`read_before_write`~~(2026-09-24 用户改名,随 #12 取消) | ~~capture_previous_on_write~~ |
| 8 | 写前读失败语义 | ~~写照常,快照不更新~~(随 #12 取消) | — |
| 9 | v0.34 范围 | **两项 P0 一并发**(r3:原三项,写前原值移出) | 仅单项 |
| 10 | 门控失败契约 | **置 last_error 三件套 + 返回 False**(r2,与现有 connect 失败契约一致) | raise TransportClosedError(触发多余的 _mark_disconnected 链) |
| 11 | **r2 改判:构造参数 → 可写属性** | 已核实全库**零个**具体子类构造函数含 retries;构造参数需穿透约 25 个 `__init__` 才可达,违反"构造仅身份"库约定。改为 `retries`/`write_retries` 同款可写属性 + 校验 setter,aio 同款转发。"现场一行关掉"的原始意图不变 | 构造参数布尔开关(r1 初版) |
| 12 | **r3:取消写前原值采集**(2026-09-24) | 用户裁决"取消写前读"——整项移出 v0.34,不入 backlog 排期;重启时按 r2 设计稿(git 历史)复用 | 随 v0.34 同发(r2 方案) |

---

## 七、风险与回退

### 风险

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| `reconnect_backoff=True` 默认开,老用户感知行为变化 | 中 | 低 | README 显著标注;`client.reconnect_backoff = False` 一行恢复 |
| 新增异常类型绕过 §3.2 规则表 | 中 | 低 | 兜底 UNKNOWN;errors.py 头部注释"新增异常必同步规则表";§4.1 有逐行短路序测试 |
| 门控状态字段在异常路径漏重置 | 低 | 中 | §2.4 生命周期表逐项对应测试;`disconnect()` 全重置兜底 |
| aio 镜像内省守卫失败 | 低 | 中 | 三个新表面全部进守卫名单(§4.1),与既有约定同流程 |
| 门控与 `_execute` 重试循环交互(retries>0 时空转) | 低 | 低 | retries 默认 0;空转有界(retries+1 次),文档标注 |

### 回退方案

- `client.reconnect_backoff = False` → 完全 v0.33.0 行为
- `last_error` 未变 → 旧调用零感知
- 极端情况整体 revert 单 commit 即可(无跨模块耦合)

---

**批准**: ✅ 设计确认(r3 待用户复审)
**下一步**:用户复审后由 writing-plans skill 出实施步骤清单
